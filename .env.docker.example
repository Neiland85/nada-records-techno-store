# Docker Compose Environment Variables Example
# Copy this file to .env.docker and modify the values
# DO NOT commit .env.docker to version control

# PostgreSQL Configuration
POSTGRES_DB=nada_records_dev
POSTGRES_USER=nada_records
POSTGRES_PASSWORD=NR_Dev_PostgreSQL_2025_$(openssl rand -hex 8)

# Redis Configuration  
REDIS_PASSWORD=NR_Dev_Redis_2025_$(openssl rand -hex 8)

# MinIO S3 Configuration
MINIO_ROOT_USER=nada_records_minio_admin
MINIO_ROOT_PASSWORD=NR_Dev_MinIO_2025_$(openssl rand -hex 8)
MINIO_DEFAULT_BUCKETS=nada-records-dev,nada-records-tracks,nada-records-covers

# Redis Commander Configuration
REDIS_COMMANDER_USER=nada_redis_admin
REDIS_COMMANDER_PASSWORD=NR_Dev_RedisCmd_2025_$(openssl rand -hex 8)

# Adminer Configuration
ADMINER_DEFAULT_SERVER=postgres
ADMINER_DESIGN=dracula

# Security Notes:
# - ALL PREVIOUS HARDCODED PASSWORDS HAVE BEEN COMPROMISED AND ROTATED
# - The values above use secure random generation with $(openssl rand -hex 8)
# - Replace $(openssl rand -hex 8) with actual generated values when copying
# - Use strong, unique passwords for each service in production
# - Consider using a password manager to generate secure passwords  
# - Never commit actual credentials to version control
# - In production, use proper secret management solutions (AWS Secrets Manager, etc.)
# - Regularly rotate all passwords and credentials

# To generate secure passwords, run: openssl rand -hex 16
